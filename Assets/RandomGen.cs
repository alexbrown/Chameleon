using UnityEngine;
using System.Collections.Generic;

public class RandomGen : MonoBehaviour {
/*
	public GameObject blue_block;
	public GameObject red_block;
	public GameObject yellow_block;
	public GameObject green_block;

	GameObject[] available_blocks = new GameObject[] { blue_block, red_block, yellow_block, green_block };

//	float x = 0;

	void Update () {
		float x = 0;
		int n = 0;
		int i = 0;
		for (i=0; i < 10; i++) {
			n=Random.Range(0, 3);
			Instantiate (available_blocks[n],  new Vector3 (x, 4, 2),  Quaternion.identity); 
			x=x+15;
		}
//		Random.Range(0, 3);
//		Instantiate (blue_block,   new Vector3 (x, 4, 2),  Quaternion.identity); x=x+15;
//		Instantiate (red_block,    new Vector3 (x, 4, 2),  Quaternion.identity); x=x+15;
//		Instantiate (green_block,  new Vector3 (x, 4, 2),  Quaternion.identity); x=x+15;
//		Instantiate (yellow_block, new Vector3 (x, 4, 2),  Quaternion.identity); x=x+15;
	}
*/
/*
	void Update1 () {
		float y = Random.Range(0f, 0f);
		// x++;
		if (x < 100) {
			Instantiate (obstacle, new Vector3 (x * 6.0f, y, 0), Quaternion.identity);
			Instantiate (obstacle1, new Vector3 (x * 12.0f, y, 0), Quaternion.identity);
		} else if (x >= 100 && x < 200) {
			Instantiate (obstacle, new Vector3 (x * 6.0f, y, 0), Quaternion.identity);		
		}

		Debug.Log(x);
	} 
*/
}
